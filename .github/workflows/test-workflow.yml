name: Test Workflow

on:
  schedule:
    - cron: '* * * * *'  # Запуск каждую минуту для проверки
  workflow_dispatch:  # Позволяет запускать workflow вручную

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Print Hello World
      run: echo "Hello, world!"
